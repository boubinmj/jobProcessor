services:
  jobprocessor:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: jobprocessor
    ports:
      - "8080:8080"
    environment:
      - WORKER_THREAD_COUNT=3
    restart: "no"
    stdin_open: true
    tty: true
